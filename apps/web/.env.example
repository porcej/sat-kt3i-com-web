# Copy to .env.local for local dev (optional — Vite proxies /api to the worker on :8787).
# VITE_API_BASE=

# Production (Cloudflare Pages): copy .env.production.example → set in Pages build env.
# VITE_API_BASE=https://sat-api.YOUR_SUBDOMAIN.workers.dev
